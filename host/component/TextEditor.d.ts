import { TextEditorInput } from './TextEditor.types';
export default function TextEditor({ editorRef, value, language, onChange, height, theme, className, readOnly, style, }: TextEditorInput): import("react").JSX.Element;
