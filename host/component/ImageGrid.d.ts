type ImageGridInput = {
    ordered?: boolean;
};
export declare const breakpoints: {
    1024: number;
    600: number;
    default: number;
};
export default function ImageGrid({ ordered, }: ImageGridInput): null;
export {};
