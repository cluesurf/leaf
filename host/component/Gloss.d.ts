import React from 'react';
export default function Gloss({ script, original, native, literal, english, }: {
    script?: string;
    original: string;
    native?: string;
    literal?: string;
    english: string;
}): React.JSX.Element;
